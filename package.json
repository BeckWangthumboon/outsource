{
  "name": "outsource-cli",
  "version": "0.1.2",
  "description": "A small CLI for Cursor-hosted Cloud Agents",
  "type": "module",
  "bin": {
    "outsource": "./src/cli.ts",
    "os": "./src/cli.ts"
  },
  "engines": { "bun": ">=1.3.0" },
  "scripts": {
    "build": "bun build ./src/cli.ts --target=bun --packages external --outfile=dist/cli.js && chmod +x dist/cli.js",
    "check": "tsc --noEmit",
    "test": "bun test",
    "verify": "bun run check && bun test && bun run build"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.23",
    "better-result": "^2.9.2",
    "commander": "^15.0.0",
    "smol-toml": "^1.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
